用了浮动以后，浏览器缩小会出现布局错乱：因为浏览器缩小以后，宽度不够，所以出现这种情况，在外面套一个具体宽度的div（.container）解决。

百分比和像素混用在浏览器缩小以后会出现留白：百分比如果是相对于body的会出现这种情况，浏览器缩小，百分比相对于浏览器的宽度也就会相应变小，但是页面中用像素值的宽度不会变，所以容易出现留白的情况。解决办法，最外面的盒子都不要用百分比来设置宽度，可以用min-width来解决，可以将min-width设置成页面中最宽的元素的宽度，让他们宽度保持一致。